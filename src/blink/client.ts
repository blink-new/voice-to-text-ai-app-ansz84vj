import { createClient } from '@blinkdotnew/sdk'

export const blink = createClient({
  projectId: 'voice-to-text-ai-app-ansz84vj',
  // authRequired: true // Removed as it's not needed for this project
})